name: kutuphane_masa_takibi
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  json_theme: ^6.0.2+3
  rive: ^0.7.16
  flutter_svg: ^2.0.7
  flutter_animated_icons: ^1.0.1
  curved_navigation_bar: ^1.0.3
  image_picker: ^1.0.0
  carousel_slider: ^4.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/images/

